spring:
  application:
    name: EmployeeRegistration
  datasource:
    url: jdbc:mysql://localhost:3306/sreekanth?useSSL=false&serverTimezone=UTC
    username: root
    password: P@ssw0rd@1914 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8050
